import React from 'react';
import Footer from '../Footer/Footer';
import Header from '../Header/Header';

const AddService = () => {
    return (
        <div>
            <Header></Header>
            This is adding a service page
            <Footer></Footer>
        </div>
    );
};

export default AddService;