import React from 'react';
import Footer from '../Footer/Footer';
import Header from '../Header/Header';

const Manage = () => {
    return (
        <div>
            <Header></Header>
            This is manage all plans
            <Footer></Footer>
        </div>
    );
};

export default Manage;